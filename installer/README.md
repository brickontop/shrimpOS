# Installer

Use the Ubuntu Server/Subiquity autoinstall file only in a disposable VM until storage has been reviewed. This live-build scaffold includes Calamares as a package, but its deployment configuration is intentionally not enabled: it must be generated from the specific target disk policy and reviewed before release. Store is opt-in by design: the update unit is disabled and never enabled by this project.
